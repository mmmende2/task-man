node_modules/
dist/
dist-web/
build/
ai_docs/
*.tsbuildinfo
*.lock
!package-lock.json
# Local Claude Code state stays ignored, but the repo's skills ship.
.claude/*
!.claude/skills/
*.swp
todo*.md
.DS_Store

# Secrets — never commit the real env/config (deploy/.env holds the tunnel
# token + CF service-token secret). The .example templates are safe.
.env
.env.*
!.env.example
.task-man/

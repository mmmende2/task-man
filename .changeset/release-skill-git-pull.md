---
---

Release skill: decide whether the droplet deploy block needs `git pull` by diffing `deploy/` and `Dockerfile` between tags, rather than shipping a hedged "only needed if…" comment on a line the operator pastes verbatim.

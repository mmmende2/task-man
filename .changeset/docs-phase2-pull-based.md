---
---

Docs: update `phase2-manual-setup-guide.md` deploy steps (bring-up, redeploy, rollback) from the retired on-box `up -d --build` flow to pull-based GHCR deploys (`TASK_MAN_TAG` + `pull`/`up -d`), with the on-box build kept as a documented emergency escape hatch.
